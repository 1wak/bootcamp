import React from "react";
import { FormLogin } from "../../components";

const Login = () => {
  return (
    <React.Fragment>
      <FormLogin />
    </React.Fragment>
  );
};

export default Login;
